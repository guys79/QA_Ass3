package System;

public class maxValueIndex_stab1 extends Program{
    // Returns index of the maximum value in the array
    // Returns -1 if array is empty
    public  int maxValueIndex(int[] arr) {
        return -1;//
    }
}
