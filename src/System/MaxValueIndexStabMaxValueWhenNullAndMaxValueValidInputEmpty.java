package System;

public class MaxValueIndexStabMaxValueWhenNullAndMaxValueValidInputEmpty extends Program{

    // Returns index of the minimum value in the array
    // Returns -1 in this case because of null
    public  int maxValueIndex(int[] arr) {
        return -1;//
    }
}
