package System;

public class MinValueMaxValueStabSumMinMaxWhenNullAndSumMinMaxWhenEmpty extends Program {

    // Returns the maximum value in the array
    //in this case is -1 because of empty array/null
    public  int maxValue(int[] arr) {
        return -1;
    }

    // Returns the minimum value in the array
    //in this case is -1 because of empty array/null
    public  int minValue(int[] arr) {
        return -1;
    }
}
