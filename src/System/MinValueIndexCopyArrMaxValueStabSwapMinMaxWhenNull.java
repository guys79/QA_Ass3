package System;

public class MinValueIndexCopyArrMaxValueStabSwapMinMaxWhenNull extends Program {
    // Returns copy of the array "arr", in this case null
    public int[] copyArr(int[] arr) {
        return null;
    }

    // Returns index of the minimum value in the array
    // Returns -1 if array is empty
    //in this case -1
    public  int minValueIndex(int[] arr) {
        return -1;
    }

    // Returns the maximum value in the array witch is -1 because of null
    public  int maxValue(int[] arr) {
        return -1;
    }
}
