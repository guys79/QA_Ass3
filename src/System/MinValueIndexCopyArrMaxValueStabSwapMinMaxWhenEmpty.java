package System;

public class MinValueIndexCopyArrMaxValueStabSwapMinMaxWhenEmpty extends Program{
    // Returns copy of the array "arr" when empty
    public int[] copyArr(int[] arr) {
        int [] array = {};
        return array;
    }

    // Returns index of the minimum value in the array
    // Returns -1 in this case because of empty array
    public  int minValueIndex(int[] arr) {
        return -1;
    }

    // Returns the maximum value in the
    // in this case returns -1 because of empty array
    public  int maxValue(int[] arr) {
        return -1;
    }
}
